import React from 'react';
import {render} from 'testing/utils';
import PreviousNextHeader from '..';

test('Render the PreviousNextHeader content', async () => {
    render(<PreviousNextHeader />);
});
