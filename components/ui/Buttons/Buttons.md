```jsx
import Button from '../Button';
<Buttons>
    <Button>Hello</Button>
    <Button>World</Button>
</Buttons>;
```

Remove left margin from first button with `flush`

```jsx
import Button from '../Button';
<Buttons flush>
    <Button>Hello</Button>
    <Button>World</Button>
</Buttons>;
```
