SticykNav Example with Custom Text

```js
// Removing backText or nextText will default the values to 'Previous' & 'Next'
<StickyNav backText={'Back to Overview'} nextText={'Complete Form'} onBackClick={() => {}} onNextClick={() => {}} />
```
