Initial state:

```js
<UploadImageProfile
    heading="Upload image heading"
    nextTextImage="Remove image"
    imgWidth={512}
    imgHeight={512}></UploadImageProfile>
```

Error state:

```js
<UploadImageProfile
    heading="Upload image heading"
    nextTextImage="Remove image"
    showError
    imgWidth={512}
    imgHeight={512}></UploadImageProfile>
```
