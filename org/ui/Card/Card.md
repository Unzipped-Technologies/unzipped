```jsx
import Action from '../Action';
import Actions from '../Actions';
<Card
    action={
        <Actions>
            <Action onClick={() => {}}>test item 1</Action>
            <Action onClick={() => {}}>test item 2</Action>
        </Actions>
    }>
    This is a Vanilla UI card, with actions
</Card>;
```
